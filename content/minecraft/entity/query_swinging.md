---
title: query(e, 'swinging')
name: query
signatures:
  - params:
      - name: entity
      - name: "'swinging'"
---

Returns `true` if the entity is actively swinging their hand, `false` if not and
`null` if swinging is not applicable to that entity.
