---
title: query(e, 'item')
name: query
signatures:
  - params:
      - name: entity
      - name: "'item'"
---

The item triple (name, count, nbt) if its an item or item frame entity, `null`
otherwise.
